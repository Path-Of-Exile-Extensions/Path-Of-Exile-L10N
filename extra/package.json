{
  "name": "@poe-vela/l10n-ext",
  "version": "0.0.1",
  "sideEffects": false,
  "files": [
    "package.json",
    "lib"
  ],
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build:types": "tsc --build",
    "build:vite": "vite build",
    "build": "npm-run-all --parallel build:vite build:types",
    "dev": "npm run build:vite -- --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.1.3",
    "rxdb": "^14.14.0",
    "vite": "^4.3.9",
    "vue-tsc": "^1.6.5",
    "vite-plugin-dts": "^2.3.0"
  },
  "dependencies": {
    "@poe-vela/core": "link:../../Vela"
  },
  "license": "ISC"
}
